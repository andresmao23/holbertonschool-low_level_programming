#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	int number = 48;
	char letra = 'a';

	for (;number < 58;number++)
		putchar(number);

	for (;letra <= 'f';letra++)
		putchar(letra);

	putchar('\n');
	return (0);

}
