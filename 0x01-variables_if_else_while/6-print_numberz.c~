#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	int number = 48;
	
	for (; number < 58 ; number++)
		putchar(number);

	putchar('\n');
	return (0);

}
