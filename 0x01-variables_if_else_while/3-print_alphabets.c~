#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	int A = 'A';
	int Z = 'Z';
	int a = 'a';
	int z = 'z';

	for (; a <= z ; a++)
		putchar(a);
	for (; A <= Z ; A++)
		putchar(A);
	putchar('\n');
	return (0);

}
