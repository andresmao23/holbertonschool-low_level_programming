#include "holberton.h"
/**
 * _abs - prints the absolute value of a number
 *
 * @n: parameter to check
 *
 * Return: Always 0 (Success)
 */
int _abs(int n)
{

	if (n >= 0)
		return (n);
	else
		return (-n);

}
